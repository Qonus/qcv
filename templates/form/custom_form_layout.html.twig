{% extends 'bootstrap_5_layout.html.twig' %}

{% block form_widget_simple -%}
    {%- set attr = attr|merge({
        'class': (attr.class|default('') ~ ' form-control-lg')|trim
    }) -%}
    {{- parent() -}}
{%- endblock form_widget_simple %}